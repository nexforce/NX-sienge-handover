# FEEDBACK, meta-agent

Append-only log of user corrections, preferences, and behavioral rules.
Most recent entries on top. Never delete, never edit past entries. Only add.

The `capture-feedback` skill appends entries automatically when the user corrects output or states a preference.
The `skill-suggester` skill reads this file weekly to propose CLAUDE.md edits and new skills.

Read the last 5-10 entries at the start of every substantive session.
Rules promoted to CLAUDE.md are marked [PROMOTED] in the original entry.

---

## 2026-05-28 | V1.5 global rollout to About Me/

**Type:** Process
**Scope:** Global
**Trigger context:** V1.4 complete, user approved promotion of cross-project rules to About Me/about-me.md after drift report.

**What the agent did:**
V1.4 rules (Session Protocol, file contract, lowercase canonical, versioning, meta-agent as meta-agent, default scheduled actions) lived only in meta-agent. Other Nexforce projects had no global instruction to enforce these patterns.

**Correction or preference:**
Promote 5 sections to /Users/vitti/Documents/Claude/About Me/about-me.md so every Claude session (any project) loads the V1.4 rules at start. Lowercase canonical folder names from now on, even where org-level instruction mentioned "Outputs/".

**How to apply going forward:**
- Every new project ships with V1.4 defaults (project-setup v1.1+)
- Every existing project should be audited against the new global rules
- The 2 default scheduled actions are recommended in every project, not optional
- All SKILL.md and subagents/*.md must declare `version:`
- Reference `meta-agent/references/self-improvement-loop.md` when explaining the system

**Promotion candidate:**
[PROMOTED] Applied to About Me/about-me.md on 2026-05-28. 5 new sections inserted between "How I work" and "Quality standard". Also fixed 3 em-dash violations in the original about-me.md (lines 23, 129, 161).

---

## 2026-05-28 | [PROMOTED] Conflito Outputs vs outputs resolvido (lowercase canonical)

**Type:** Process
**Scope:** Global
**Trigger context:** Structural audit detected a conflict: organization-level CLAUDE.md mentions capitalized "Outputs/", project-setup canonical uses lowercase "outputs/". 4 canonical files in meta-agent still used capitalized "Outputs/": design-agent-architecture/SKILL.md, build-eval/SKILL.md, compress-session/SKILL.md, agent-architect.md.

**What the agent did:**
Inconsistent pattern. Some places used "Outputs/" (capitalized), others "outputs/" (lowercase). The project-setup HARD CONSTRAINT already forced lowercase, but references inside skills and subagents were not aligned.

**Correction or preference:**
Canonical is lowercase: `outputs/`, `inputs/`, `references/`, `templates/`, `skills/`, `subagents/`. Rationale: aligns with Unix convention, with the project-setup HARD CONSTRAINT, and with the existing meta-agent structure (skills/, subagents/, references/, templates/, all lowercase).

**How to apply going forward:**
- Always use lowercase for canonical folder names
- If the organization-level CLAUDE.md (under About Me/) shows capitalized Outputs/, the canonical project rule (lowercase) prevails for all artifacts generated by meta-agent
- For older projects with capitalized Outputs/ (vitti-interviewer, guardiao-otimizacao), keep for compatibility until migrated via optimize-project

**Promotion candidate:**
[PROMOTED] Already a HARD CONSTRAINT in project-setup. Add an explicit note in meta-agent CLAUDE.md if the conflict with organization-level resurfaces.

---

## 2026-05-28 | [PROMOTED] FEEDBACK.md created with P0 rollout

**Type:** Process
**Scope:** Global
**Trigger context:** Vitti requested a structural overhaul to add a feedback layer, skill suggester, and a self-improvement loop to every Nexforce project

**What the agent did:**
Until V1.3, meta-agent had no persistent feedback layer. User corrections were re-learned every session unless manually added to CLAUDE.md. No skill audited feedback patterns or proposed improvements automatically.

**Correction or preference:**
Every Nexforce project must have FEEDBACK.md as a first-class persistent log alongside MEMORY.md. Two new mandatory skills (`capture-feedback`, `skill-suggester`) join `token-budget` and `compress-session` as pre-installed defaults. Two default scheduled actions (`weekly-skill-audit`, `monthly-claude-md-review`) recommended on every new project.

**How to apply going forward:**
- On every new project: project-setup creates FEEDBACK.md + 4 mandatory skills + recommends 2 default scheduled actions
- Within sessions: invoke capture-feedback whenever the user corrects output
- Weekly: skill-suggester runs (manually or scheduled) to surface patterns
- Monthly: feedback-analyzer subagent proposes CLAUDE.md edits

**Promotion candidate:**
[PROMOTED] Codified in project-setup SKILL.md and the new skills. CLAUDE.md and README.md updated to list the new defaults.

---
